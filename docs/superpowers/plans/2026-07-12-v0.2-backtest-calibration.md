# v0.2 Universe + Store + Backtest Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Real NSE universes, persistent DuckDB run store, and a point-in-time backtest harness reporting IC / decile spread / hit rate for our scores.

**Architecture:** per v0.2 spec — `data/universe_nse.py`, `storage.py`, `backtest/{pointintime,harness}.py`, CLI wiring. Pure computation split from fetch for testability.

**Tech Stack:** + duckdb, scipy (Spearman). Everything else unchanged.

---

### Task 1: NSE universe ingestion
**Files:** `src/mbe/data/universe_nse.py`, `tests/test_universe_nse.py`
- [ ] RED: parse fixture CSV text ("Company Name,Industry,Symbol,Series,ISIN Code" rows) -> ["X.NS", ...]; unknown universe raises KeyError; cache hit avoids downloader call (inject fake fetcher).
- [ ] GREEN: `NSE_SOURCES = {"nifty500": url, "nifty-midcap150": url, "nifty-smallcap250": url, "nifty-microcap250": url}` (niftyindices.com archives), `fetch_universe(name, cache, fetcher=default_http) -> list[str]`, 7-day TTL via DiskCache JSON, browser UA. ProviderError on failure/empty.
- [ ] Wire into `universe.get_universe`: dynamic names resolved via fetch_universe; `mbe universes` lists both. Commit.

### Task 2: DuckDB store
**Files:** `src/mbe/storage.py`, `tests/test_storage.py`; dep: duckdb
- [ ] RED: save_run(screen_result, universe, db_path) returns run_id; history(ticker) returns rows with as_of + scores; two runs -> two rows; save_backtest + load summaries.
- [ ] GREEN: schema per spec; JSON columns via json.dumps; idempotent CREATE TABLE IF NOT EXISTS. Commit.

### Task 3: Point-in-time module
**Files:** `src/mbe/backtest/pointintime.py`, `tests/test_pointintime.py`
- [ ] RED: availability_date(fy=2024, fy_end_month=3) == 2024-06-29(+90d); truncate_financials keeps only available years; truncate_prices exact cut; sanitize_info nulls insider/institution/market_cap and re-derives market_cap = shares x cutoff price.
- [ ] GREEN: implement; fiscal-year-end month defaults to 3 (India) via ticker suffix else 12. Commit.

### Task 4: Backtest harness
**Files:** `src/mbe/backtest/harness.py`, `tests/test_harness.py`
- [ ] RED (pure math first): `evaluate_cutoff(scores: dict[ticker, float], fwd_returns: dict[ticker, float], quantiles=5)` -> CutoffResult(ic, top_q_mean, bottom_q_mean, spread, hit_rate, n). Perfect monotone -> ic==1; reversed -> ic==-1.
- [ ] GREEN: scipy.stats.spearmanr; quantile split by rank.
- [ ] RED: `analyze_as_of(ticker, provider, cutoff)` uses truncations + sanitized info (assert via StubProvider that post-cutoff years/prices never reach engines; insider None).
- [ ] GREEN: fetch 10y prices once, truncate; compute forward return from cutoff to cutoff+horizon within same frame.
- [ ] `run_backtest(tickers, provider, cutoffs, horizon_days, score="multibagger") -> BacktestReport` (per-cutoff results + mean IC + skipped tickers w/ reasons) + `render_backtest_md`. Test with stub provider on synthetic trending data. Commit.

### Task 5: CLI wiring
**Files:** `src/mbe/cli.py`
- [ ] `snapshot UNIVERSE`, `history TICKER`, `backtest UNIVERSE --cutoffs --horizon --score --limit`. Smoke via `--help` in tests? (existing pattern: direct function tests suffice). Commit.

### Task 6: Live verification
- [ ] `mbe universes` shows dynamic lists; fetch nifty-midcap150 live.
- [ ] `mbe snapshot india-midsmall` then `mbe history MCX.NS` shows the run.
- [ ] Real backtest: `mbe backtest nifty-midcap150 --limit 60 --cutoffs 2024-07-01,2025-07-01 --horizon 365`. Inspect report sanity; commit fixes; CHANGELOG v0.2.0; memory update.

## Self-review
Spec coverage: ingestion→T1, store→T2, PIT→T3, harness/IC→T4, CLI→T5, live→T6 ✓. No placeholders ✓. Names consistent (fetch_universe, save_run, truncate_financials, run_backtest) ✓.
